const menuBtn = document.getElementById('menu-btn');
const navbarMenu = document.getElementById('navbar-menu');

menuBtn.addEventListener('click', ()=> {

        navbarMenu.classList.toggle('displayNav')

})